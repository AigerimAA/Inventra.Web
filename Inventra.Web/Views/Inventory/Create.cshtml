@using Inventra.Application.Inventories.Commands.CreateInventory
@model CreateInventoryCommand

@{
    ViewData["Title"] = "New Inventory";
}

<div class="row justify-content-center">
    <div class="col-md-7">
        <h2 class="mb-4">@L["NewInventory"]</h2>

        <form asp-action="Create" method="post">
            <div asp-validation-summary="ModelOnly" class="alert alert-danger"></div>

            <div class="mb-3">
                <label asp-for="Title" class="form-label">@L["Title"]</label>
                <input asp-for="Title" class="form-control" placeholder="e.g. Office Equipment" />
                <span asp-validation-for="Title" class="text-danger"></span>
            </div>

            <div class="mb-3">
                <label asp-for="Description" class="form-label">@L["Description"]</label>
                <textarea asp-for="Description" class="form-control" rows="3"
                          placeholder="Describe your inventory..."></textarea>
            </div>

            <div class="mb-3">
                <label asp-for="CategoryId" class="form-label">@L["CategoryLabel"]</label>
                <select asp-for="CategoryId" class="form-select"
                        asp-items="ViewBag.Categories">
                    <option value="0">@L["SelectCategory"]</option>
                </select>
                <span asp-validation-for="CategoryId" class="text-danger"></span>
            </div>

            <div class="mb-3 form-check">
                <input asp-for="IsPublic" class="form-check-input" />
                <label asp-for="IsPublic" class="form-check-label">
                    @L["Public"]
                </label>
            </div>

            <div class="d-flex gap-2">
                <button type="submit" class="btn btn-primary">@L["Create"]</button>
                <a asp-action="Index" class="btn btn-outline-secondary">@L["Cancel"]</a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @await Html.PartialAsync("_ValidationScriptsPartial")
}